/*int[,] numerosInteiros = new int[3, 2];
int soma = 0;
int media = 0;

for (int linha = 0; linha < 3; linha++)
{
    for (int coluna = 0; coluna < 2; coluna++)
    {
        Console.Write("Digite o número: ");
        numerosInteiros[linha, coluna] = int.Parse(Console.ReadLine()!);
    }
}

for (int linha = 0; linha < 3; linha++)
{
    for (int coluna = 0; coluna < 2; coluna++)
    {
        soma = soma + numerosInteiros[linha, coluna];
    }
}

media = soma / 6;
Console.WriteLine($"Média: {media}");
*/